// Image uploader component rendered on base app. Takes image uploader input and processes it. 
// Sends image to image card rendered on upload results


import React, { FunctionComponent } from 'react';

type Props = {
    //
};

const 

const ImageUploader: React.FC = (): Function => {
    return (
    <div>
        Upload Image here: <button></button>
    </div>
    );
};

export default ImageUploader;
